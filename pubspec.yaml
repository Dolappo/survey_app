name: survey_challenge
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"


dependencies:
  flutter:
    sdk: flutter


  google_fonts: ^3.0.1
  cupertino_icons: ^1.0.2
  flutter_spinkit: ^5.1.0
  rflutter_alert: ^2.0.4
  provider: ^6.0.3
  stacked: ^2.3.13
  stacked_services: ^0.9.2
  build_runner: ^2.1.11
  stacked_generator: ^0.5.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

flutter:

  uses-material-design: true
  assets:
    - images/
